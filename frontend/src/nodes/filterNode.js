// filterNode.js
// Node for filtering data based on conditions
import { createThemedNode } from "./nodeFactory";
import { nodeConfigs } from "./nodeConfigs";

export const FilterNode = createThemedNode(nodeConfigs.filter);
