// conditionalNode.js
// Node for conditional logic (if/else branching)
import { createThemedNode } from "./nodeFactory";
import { nodeConfigs } from "./nodeConfigs";

export const ConditionalNode = createThemedNode(nodeConfigs.conditional);
